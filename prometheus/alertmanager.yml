route:
  receiver: default
  group_by: [alertname, job]
  group_wait: 10s
  group_interval: 1m
  repeat_interval: 30m

receivers:
  - name: default
    webhook_configs:
      - url: http://alert-webhook:5001/alerts
        send_resolved: true
